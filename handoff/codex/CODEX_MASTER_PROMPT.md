# Codex Master Contract

具体命令以项目根目录的 `START_HERE_CODEX.md` 和 `runbook/` 为准。

开始：当前目录必须是 `E:\project\OpenClaw_VideoFactory`；阅读START、STATUS和AGENTS；第一次只执行P0；不得使用旧版`工作区/`路径。

架构：OpenClaw长期编排；Codex实施；SQLite状态；job产物；Remotion+FFmpeg主成片；AI TTS；Whisper字幕；ComfyUI增强；剪映P5；OpenClaw飞书Channel入站；lark-cli交付；抖音人工发布。

安全：OpenClaw配置先查实时Schema；示例片段不得整文件覆盖；exec mode auto；密钥用SecretRef/本机安全存储；第三方先审查；模型先批准；媒体不可信；不自动发布。

阶段：P0环境/通道；P1人工主题MP4；P2每日选题和12:00立即兜底（没有固定10分钟窗）；P3 GPU；P4参考视频；P5剪映。

工程：任务对应Backlog ID；阶段分支；提交含测试；运行acceptance gate；只有证据才能更新STATUS；所有命令、版本、日志、产物、限制和回滚写reports；不得把计划说成完成。

停止等待用户：授权、管理员权限、升级、驱动、模型、扩权、许可证、预算、剪映切换和项目外删除。
