# RC High-Pass Speech-Cue and Knowledge-Density Plan

**Goal:** Turn summary-card animation into measured speech events, expose the related technical facts on screen, and verify V8 from the actual Jianying projects root.

1. Write failing tests for ordered semantic cues, non-modulo animation, subsegment VoiceOver import, and sequential-frame sampling.
2. Split summary narration into seven local TTS subsegments; persist six ordered cue timestamps in the timing manifest.
3. Render the three main cards only at their matching phrase starts and the design-check row at later narrated design phrases.
4. Add transfer function, impedance boundary cases, cutoff/phase lanes, time-shift relationship, and design steps to visible cards.
5. Re-render V8, run direct/decoded critical-frame checks and all-frame scan, then create an audible E-drive Jianying draft plus one visible C-to-E junction.
6. Run regression suites, update Obsidian, commit, and push without phase promotion.
