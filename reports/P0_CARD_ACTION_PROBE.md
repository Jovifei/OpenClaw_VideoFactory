# 016 card probe

`NOT_RUN`.

The probe was intentionally stopped before creating or sending a fake card. The current OpenClaw dispatch would convert it to synthetic text and route it to the existing Router; that cannot produce valid evidence for `inbound_claim` ownership or trusted callback metadata. No user upload, Analyzer call, or live Feishu action was requested by this task.
