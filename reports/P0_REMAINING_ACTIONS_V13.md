# Remaining actions V13

Do not proceed automatically. A separate authorization is required for a maintenance-window implementation that adds durable storage, official SDK transport, local authenticated OpenClaw bridge, and controlled single-consumer cutover. Then rerun R0-R3 sequentially; R4/R5 remain gated by R3.
