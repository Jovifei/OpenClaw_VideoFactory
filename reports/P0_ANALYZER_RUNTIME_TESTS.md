# Analyzer Runtime Tests

Current offline execution passed 23/23 Analyzer MCP contract tests. The tests cover all three tools, six-field input enforcement, receipt/hash binding, quarantine and content-parsed gates, kind routing, idempotency, traversal, outside-storage and reparse rejection, bounded video artifacts, and the no-text-fallback rule.

The 008 local runtime report remains the evidence for the already-installed CUDA faster-whisper, ffprobe, bounded frame, audio extraction, and multimodal fixture route. The test-mode flag is confined to the contract test process and is absent from production configuration. Real R3-R5 still requires the user’s uploads.
