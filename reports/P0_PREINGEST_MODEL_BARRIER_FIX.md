# P0 pre-ingest model barrier

Status: `blocked_global_change_approval_required`.

No code, Skill, OpenClaw configuration, runtime, model, binding, Gateway, OAuth, Cron, P0 gate, commit, or tag was changed. O2 remains `blocked_missing_existing_json5_parser`; no parser was installed. The required barrier must use a global OpenClaw plugin activation to claim the attachment before the core media-understanding call. The exact proposed scope is in [P0_PREINGEST_BARRIER_ESCALATION.md](P0_PREINGEST_BARRIER_ESCALATION.md).

The old PNG remains failed evidence. The old TXT acceptance is conditional and needs a new upload only after a future approved barrier implementation passes its local tests.
