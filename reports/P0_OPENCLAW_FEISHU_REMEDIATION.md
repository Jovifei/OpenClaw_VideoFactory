# P0 OpenClaw, Codex, and Feishu remediation evidence

Observed: 2026-07-12, Asia/Shanghai. Scope remains P0 only. No model or node was downloaded, no driver was changed, no formal Cron entry was registered, no Jianying automation was enabled, and no Douyin action was taken.

| Step | Command | Actual result | Evidence | Risk | Rollback |
|---|---|---|---|---|---|
| Global-config backup | Secure copy of `C:\Users\Admin\.openclaw\openclaw.json` to the user-profile backup directory, with SHA-256 comparison | Exit 0; backup hash matched; no config content or secret was stored in the repository. | `reports/OPENCLAW_CONFIG_BACKUP.md`, `reports/command_logs/48_openclaw_config_backup.txt` | Global state is involved. | Restore the recorded backup only with explicit user approval. |
| Isolated agent | `openclaw agents add video-factory --workspace "E:\project\OpenClaw_VideoFactory" --non-interactive --json` | Exit 0; created a non-default project-scoped agent with no initial bindings. | `reports/command_logs/51_add_video_factory_agent.txt` | Adds an idle OpenClaw agent. | Delete the `video-factory` agent only with explicit approval. |
| Gateway and plugin hardening | `openclaw config patch --file reports/openclaw_state/p0_minimal_patch.json5 --dry-run`, then apply; validated allowlist patch for `codex-supervisor`; `openclaw config validate`; `openclaw gateway restart`; `openclaw plugins inspect codex-supervisor` | All recorded commands exited 0. Gateway is loopback-bound and the Codex Supervisor is loaded. Existing Cron entries were not changed. | `reports/command_logs/52_openclaw_minimal_patch_dry_run.txt`, `53_openclaw_minimal_patch_apply.txt`, `54_config_validate_after_patch.txt`, `54_gateway_restart_after_patch.txt`, `54_gateway_status_after_patch.txt`, `59_codex_supervisor_allowlist_dry_run.txt`, `60_codex_supervisor_selection.txt`, `61_codex_supervisor_inspect.txt` | Global gateway/plugin changes affect other OpenClaw agents. | Reapply the verified pre-P0 backup with explicit approval, then restart the gateway. |
| Local-skill visibility | `openclaw skills check --agent video-factory --json` | Exit 0; all 14 required project skills reported visible. | `reports/command_logs/62_skills_check_summary.txt`, `reports/SKILL_VISIBILITY.json` | Read-only check. | None. |
| Official Feishu reuse attempt | `(echo y)\|openclaw channels login --channel feishu --account video-factory` | Exit 0 and displayed “Bot configured”, but this installed version only reused existing config and did not create `channels.feishu.accounts.video-factory`. | `reports/command_logs/64_feishu_channel_login_reuse_existing_bot.txt`, `reports/command_logs/66_feishu_account_structure_sanitized.txt` | A false success could lead to a duplicate consumer or a dead route. | No credential or app configuration was written for `video-factory`. |
| Temporary binding verification | `openclaw agents bind --agent video-factory --bind feishu:video-factory --json`; `openclaw gateway restart`; `openclaw channels status` | All commands exited 0, but channel status did not list `video-factory`; the binding pointed to a nonexistent account. | `reports/command_logs/65_bind_video_factory_feishu.txt`, `65_gateway_restart_after_video_factory_bind.txt`, `65_channels_status_after_video_factory_bind.txt` | Dangling route; no known inbound delivery target. | Removed in the next step. |
| Child audit and parent review | `Invoke-ChildClaude` with one named source file, `Read,Glob,Grep`, 60-second timeout; then direct source inspection | Child returned `Success=false`, `TimedOut=true`, zero stdout/stderr bytes. Its output was rejected. Parent source review found the existing-bot branch returns the existing config and the new-app branch resolves the default account rather than the requested override. | `reports/command_logs/67_child_claude_feishu_login_audit.txt`, `reports/child_claude_feishu_login_audit_diagnostic.json`, `reports/command_logs/68_agents_list_sanitized.txt` | Child timeout must not be treated as evidence. | None; read-only. |
| Remove invalid route | `openclaw agents unbind --agent video-factory --bind feishu:video-factory --json` | Exit 0; removed the only `video-factory` Feishu binding. Existing agents and their bindings were not modified. | `reports/command_logs/69_remove_dangling_video_factory_binding.txt` | Project agent has no inbound channel until a dedicated bot is provisioned. | Recreate only after a real `channels.feishu.accounts.video-factory` configuration exists and is independently checked. |

## Current P0 boundary

- Passed: package checks, machine preflight, loopback gateway state, Codex CLI login, loaded Codex Supervisor, and project-agent skill visibility.
- Not passed: a project-specific Feishu account, inbound text/file test, lark-cli authorization, outbound file/message smoke, and Feishu-executed `/status`/Codex commands.
- Blocking fact: each existing Feishu account is already bound to another agent. Reusing one would violate the single-consumer requirement. The current OpenClaw version cannot create a named account from an existing bot through `channels login --account` without copying credentials; its source behavior was directly inspected.

## Safe next configuration plan (not executed)

1. Provision a dedicated Feishu bot/app for `video-factory` using an approved interactive flow, with `dmPolicy: pairing` or an explicit allowlist and a restrictive group policy.
2. Configure that dedicated app only at `channels.feishu.accounts.video-factory`, validate, and verify it appears in `openclaw channels status` before binding.
3. Bind that one account to `video-factory`, then perform user-observed inbound `/status` plus a harmless file receipt test.
4. Configure lark-cli under a separate named profile and perform an allowlisted, idempotent outbound smoke only after the target is confirmed.

The four steps above remain P0 work but require a dedicated bot credential or interactive authorization. They must not be replaced by copying an existing account or rebinding any existing agent.
