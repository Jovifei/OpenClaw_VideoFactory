# P0 managed restart execution 044

No managed restart was executed.

The conditional authorization required both a proven authentication deadlock in
the prior safe restart and a passing fresh protection preflight. Neither was
available. No `--safe` replay, ordinary restart, recovery start, force flag,
manual process operation, Core stop, Project Gateway start, or Feishu action
occurred.

Observed unchanged listener PID: `13144`.
