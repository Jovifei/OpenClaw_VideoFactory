# P0 pre-ingest model-barrier tests

Status: `not_run_global_change_approval_required`.

No fake-model suite was created or executed because a testable implementation cannot be activated without the prohibited global OpenClaw plugin configuration change. The earlier 32/32 media-ingest result remains historical evidence only; it is not evidence for the missing pre-model barrier.

The next approved implementation must add the fifteen specified attachment/no-attachment cases and record all six required barrier fields. It must also re-run the existing 32 media-ingest tests, text-chat regression, single-consumer route check, syntax/secret checks, and manifest/config comparisons.
