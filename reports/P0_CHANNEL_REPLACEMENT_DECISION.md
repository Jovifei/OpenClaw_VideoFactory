# Channel replacement decision

`PROJECT_OWNED_FEISHU_GATEWAY_FEASIBLE`

Choose Candidate B: a project-owned Feishu Gateway with one long-connection consumer and a local bridge to existing OpenClaw Agent/session runtime. Candidate A is rejected as `OFFICIAL_PLUGIN_CANNOT_REPLACE_CORE_CHANNEL`. No production configuration, Binding, Agent, Cron, model, Gateway process, or OpenClaw core source was changed.
