# Remaining Actions V31

1. Do not retry pairing under 047 retry authorization; its one connection has been consumed.
2. If diagnosis is desired, obtain separate authorization for a **read-only protocol rejection audit** that preserves the current blocked private transaction and does not create another pairing request.
3. If repository-wide removal or inspection of the four historical Shadow fixture paths is required, authorize it separately; this task did not read or modify those paths.
4. Do not approve devices, request elevated scopes, use shared tokens, start the Project Gateway, alter Core Feishu, or enter zhongshu migration without later explicit authorization.
