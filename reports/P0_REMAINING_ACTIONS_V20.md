# P0 Remaining Actions V20

1. Resolve a no-network Shadow load of the installed Feishu plugin without
   invoking automatic install or external network access.
2. Re-run Shadow Gateway/RPC with Feishu transport disabled and prove plugin
   loaded plus `zhongshu` account status.
3. Execute Shadow-only account `channels.stop` and `channels.start`; capture
   abort/close, consumer zero/one, and non-target invariance.
4. Only after those pass, create the WhatIf/Execute production-guarded control
   scripts and re-run the full test/evidence matrix.
5. Obtain explicit maintenance-window authorization separately before any real
   stop, Project Gateway start, or Feishu test.

No production action is pending automatically.
