# PHASE1-REFERENCE-VIDEO-ANALYSIS-001

## Boundary

Result target: `PHASE1_LOCAL_REFERENCE_REVIEW_PACKAGE_READY`.
`PROJECT_STATUS.yaml` remains unchanged at its current formal Phase 1 status.
The branch uses only local MP4 input, FFmpeg/ffprobe, the offline
PySceneDetect sidecar, optional cached faster-whisper, and the existing
`run_local_brief()` → `run_job()` → `build_review_package()` chain. No remote
provider, Feishu, Cron, model download, automatic publication, source frame,
or source audio reuse is enabled.

## Contracts and provenance

- `reference_receipt.json`, `reference_rights.json`, `reference_report.json`,
  `original_brief.json`, and `difference_report.json` are strict versioned
  contracts.
- Rights evidence requires `rights_basis`, `source_owner`,
  `license_reference`, source SHA-256, and processing timestamp.
- The private source copy is `input/reference_videos/<sha256>.mp4`, ignored by
  Git and made read-only after source/copy/source-recheck/destination hash
  verification.
- Runtime evidence is stored under ignored
  `state/phase1_local/reference_jobs/`; only sanitized JSON documents are
  copied to a local-reference review package.

## Analysis policy

PySceneDetect `0.7.1` uses `ContentDetector` threshold `27` and a minimum
scene length of `0.8` seconds. The report contains scene times, representative
time points, median shot duration, density, and a three-level pace label. A
complete cached faster-whisper `1.2.1` small snapshot is loaded in CPU `int8`
offline mode; missing cache or no audio records `unavailable` and does not
block the original render.

## Verification evidence

The synthetic local reference run generated by FFmpeg completed the full
workflow and produced a 1080×1920, 30 FPS, H.264/AAC master plus an 11-artifact
review package. The package includes all five reference evidence documents and
keeps visual/logo/face/perceptual/shot-order checks as
`human_review_required`. The audio synthetic run also exercised the offline
faster-whisper adapter and recorded `asr.status=available` without network
access.

Targeted regression evidence:

```text
python -m pytest tests/phase1_local -q       17 passed
python -m pytest tests/reference -q          6 passed, 1 symlink skip
python -m pytest tests/director -q            47 passed
python -m pytest tests/video -q               273 passed
python -m pytest video_factory/tests -q       5 passed
```

Fresh-clone evidence was saved to
`E:/Claude_allow/Download/OpenClaw_VideoFactory-phase1-reference-fresh-clone-20260821.json`.
The clone checked out commit `6536ab94ba4d45489b37f36babb3580e8fdd0dcb`,
installed the locked wheels with `--no-index`, regenerated the ignored legacy
video fixtures, and passed the same 17/6+skip/47/273/5 core suites. Its
synthetic reference run produced source SHA
`d707d6add89556d272be5858ed02c60ef3cdb24d2d3004529fb16dc6e56a0b11`, output
SHA `d578bc3e8e837392c6b41dfa5756e8d43aecd646ac2134363ae9ca34bde08981`, an
11-artifact package, and ffprobe-confirmed 1080×1920/30 FPS/H.264/AAC media.

This report is not a publication or authorization decision; Jovi must still
perform the final human review of any real authorized source.
