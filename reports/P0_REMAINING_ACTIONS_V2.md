# P0 Remaining Actions V2

1. Upload only `tests/fixtures/feishu_delivery/p0-image-test.png` to the dedicated group and complete the single-file ingress receipt/hash/consumer check.
2. After PNG confirmation, upload `p0-video-test.mp4` and complete the same MP4 checks.
3. Perform the separately authorized actual lark-cli Markdown/PNG/TXT/MP4 egress and idempotency proof; do not infer this from dry-run.
4. Refresh existing Agent/Binding/Cron regression evidence and prove no VideoFactory production Cron.
5. Run the corrected final P0 Gate once. Only exit 0 may permit status advance, commit, tag and P1 branch.

Do not resume Codex CLI maintenance unless Jovi sends `开始Codex CLI维护窗口`. Do not investigate the optional OpenClaw Codex Plugin OAuth.
