# P0 Remaining Actions V3

Status: `conditional_not_passed`.

1. Separately authorize a read-only feasibility review of the Channel pre-agent middleware option; do not retry the blocked plugin-owned migration unchanged.
2. Resume sequential real PNG then MP4 ingress only under their existing dedicated acceptance contracts.
3. Authorize actual lark-cli egress separately after its dry-run-only boundary is accepted.
4. Resolve the O2 parser availability only through a separately approved environment change; its status remains `blocked_missing_existing_json5_parser`.
5. Run the final P0 Gate only when every required real-chain item is independently complete.
