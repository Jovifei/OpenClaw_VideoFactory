version: 2.4
project_root: E:\project\OpenClaw_VideoFactory
status: phase_1_topic_only_in_progress
last_updated: 2026-08-31
current_phase: PHASE_1_LOCAL_VIDEO_FACTORY
phase_model: user_aligned_2026_08_14
product_phases:
  PHASE_1_LOCAL_VIDEO_FACTORY:
    status: in_progress
    goal: topic-only local qualification toward a 16:9 audible preview and Jovi-reviewed Jianying draft, while preserving legacy local-reference and authorized-public-research capabilities
    excludes:
      - Feishu
      - OpenClaw runtime and daily state
      - Cron
      - automatic topic selection
      - automatic publication
    canonical_document: docs/PRODUCT_PHASES.md
  PHASE_2_FEISHU_AUTOMATION:
    status: blocked_by_PHASE_1_LOCAL_VIDEO_FACTORY
    goal: Feishu safety, topic cards, user selection, qualified 12:00 fallback, controlled delivery, and Cron after non-scheduled proof
    prerequisite: PHASE_1_LOCAL_VIDEO_FACTORY
# Historical P0-P5 labels below are retained for existing evidence and tooling.
# They are compatibility tracks, not the product-stage order, and P0 no longer
# blocks the local Phase 1 video factory.
phases:
  PACKAGE:
    status: passed
    evidence:
      - reports/package_release_validation.json
  P0: {status: not_started, evidence: []}
  P1: {status: blocked_by_P0, evidence: []}
  P2: {status: blocked_by_P1, evidence: []}
  P3: {status: blocked_by_P2, evidence: []}
  P4: {status: blocked_by_P3, evidence: []}
  P5: {status: blocked_by_P4, evidence: []}
exceptions:
  - id: PINK_PIG_VIDEO_FACTORY_PRODUCTIZATION_001
    scope: PHASE_1_LOCAL_VIDEO_FACTORY
    granted_by: user
    summary: 小粉猪视频工厂产品化 Phase1（Registry + Schema + Storyboard 编译 + 多场景 Demo）
    note: 本轮属于本地 Phase 1 产品工作，产出不构成 Phase 1 通过依据；历史 P0 飞书门禁移至 Phase 2
    does_not_imply_phase_pass: true
    allowed_paths:
      - video_factory/
      - assets/
      - schemas/video/
      - src/factory/assets/pink_pig/
      - external/
      - docs/
      - tests/video/
  - id: PHASE1-LOCAL-FACTORY-COMPLETION-002
    scope: PHASE_1_LOCAL_VIDEO_FACTORY
    granted_by: user
    summary: Phase 1 人工审阅、证据对账和正式 Gate 收口
    note: 只补充本地 Phase 1 验收合同和工具；不构成 Phase 1 通过，不进入飞书或 Cron
    does_not_imply_phase_pass: true
    allowed_paths:
      - PROJECT_STATUS.yaml
      - docs/PRODUCT_PHASES.md
      - schemas/video/
      - src/factory/phase1_acceptance.py
      - src/factory/phase1_gate.py
      - scripts/phase1_acceptance.py
      - scripts/phase1_gate.py
      - tests/phase1_acceptance/
      - runbook/11_PHASE1_COMPLETION.md
      - handoff/codex/PHASE1_COMPLETION_PROMPT.md
      - reports/PHASE1_REMOTE_AUDIT_20260822.md
known_implementation_gaps:
  - Phase 1 remains in progress; no formal human review, acceptance manifest, prereview set, or final Gate has passed
  - live MPT I2C topic was rejected twice at score 78; it is not qualified evidence
  - content visuals still need editorial review
  - final Jovi Jianying review, fresh-clone verification, and read-only formal Gate are pending
  - topic_only_v1 still needs Flash/watchdog, FreeRTOS, I2C, and a distinct live-topic prereview with explicit approved reviews
  - cancel, retry, controlled restart recovery, and CPU/NVENC fallback need fresh unified evidence
  - one Jovi-authorized real local reference video still needs human originality review
  - Phase 2 Feishu runtime credentials and target IDs are not configured
  - Phase 2 production Cron is not registered
rules:
  - never mark a phase passed without evidence
  - never register production cron before Phase 2 passes
  - never download models without approval and budget check
  - never publish to Douyin automatically
