# P0-R3-REAL-RESULT-RETEST-061

## Goal

Perform one fresh, user-driven R3 image qualification in the original Feishu
group after the 055 result-reply remediation. Prove the complete image path
returns one useful, redacted analysis reply—not the old generic completion
notice. This task is the only live prerequisite before R4, R5 and the P0 Gate.

## Frozen boundaries

- No code, configuration, Binding, Agent, Cron, OAuth, model, Gateway or Core
  lifecycle change.
- Project Gateway remains stopped; Core Feishu `zhongshu` remains the only
  consumer.
- No retry of a failed live event, no ticket reuse, no R4/R5, no Gate, no
  commit/push/tag.
- Do not store Ticket plaintext, message IDs, paths, SHA-256 values, user IDs
  or secrets in reports or Obsidian.

## 061A — Read-only readiness check

After Jovi explicitly says `开始 P0-R3-061 真实复测`, collect a redacted
baseline only:

1. Core Gateway and the `zhongshu` Core Feishu account are running.
2. Project Gateway process/listener count is zero.
3. `MEDIA_TICKET_EXECUTION_ENABLED=true`; no active or unknown media work; no
   pending image Ticket for the same chat and sender.
4. Current source and configuration SHA match the frozen 055 baseline.
5. The 055 result presentation mapping is present; no generic successful-image
   fallback is enabled.

Any failure writes `R3_RETEST_NOT_STARTED:<layer>` and stops.

## 061B — Jovi performs one upload

Jovi manually sends exactly one file, with no caption or additional text:

`E:\project\OpenClaw_VideoFactory\tests\fixtures\feishu_delivery\p0-image-test.png`

The runtime must ingest once, quarantine safely, preserve source/stored hash
equality, create one five-minute opaque image Ticket and send one Ticket reply.
The Ticket plaintext remains only in the original Feishu group.

On success record only a redacted Ticket reference and stop at
`WAITING_FOR_R3_061_COMMAND`.

## 061C — Jovi performs one exact command

Jovi copies the newly issued Ticket into a new message containing only:

```text
/vf image <new-ticket>
```

The runtime must prove, with redacted evidence:

1. Ticket was pending, mature, chat/sender/media/action bound and atomically
   consumed once.
2. Receipt remains quarantined; stored SHA is rechecked; one
   `analysis_request` and one image Analyzer invocation occur.
3. Server-side dispatch selects `xiaomimimo/mimo-v2.5`; no
   `mimo-v2.5-pro` fallback occurs.
4. The original group receives exactly one reply beginning `图片分析结果：`,
   containing readable analysis content and no paths, SHA values, IDs or
   secrets.
5. `媒体处理已完成。` is not accepted as a successful R3 reply.

## 061D — Evidence and stop rule

Write redacted evidence to:

- `reports/P0_REAL_R3_RETEST_061.md/.json`
- `reports/P0_REAL_R3_REPLY_QUALITY_061.md`
- `reports/P0_CURRENT_STATUS_V40.md`
- `reports/P0_EVIDENCE_INDEX_V40.md`
- `reports/P0_REMAINING_ACTIONS_V40.md`
- `reports/NEXT_USER_ACTION.md`

Outcomes:

- `R3_IMAGE_ANALYSIS_OK` / `READY_FOR_R4_AUDIO` only when all 061C checks pass.
- `R3_PARTIAL_PASS:RESULT_REPLY_TOO_THIN` for completed backend work without a
  useful visible result.
- `R3_FAILED:<layer>` for every other failure; freeze evidence and do not ask
  Jovi to resend in the same task.

## Human review remaining after 061 planning

- Jovi reviews the offline P1 candidate MP4/WAV/SRT/contact-sheet evidence;
  this is not a phase gate.
- Jovi performs the single fresh R3 upload and exact command when ready.
