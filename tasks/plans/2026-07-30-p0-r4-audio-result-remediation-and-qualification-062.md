# P0-R4 Audio Result Remediation and Qualification 062

## Goal

Replace the existing generic successful-audio completion notice with a
server-owned, redacted transcript reply. Prove it offline first. A real R4
WAV qualification remains prohibited until Jovi explicitly sends
`开始 P0-R4-062 真实验证`.

## 062A — Offline remediation

1. Add a server-owned audio presentation that reads only a completed
   `jobs/.../transcript.json` generated by `transcribe_audio`.
2. Return the bounded reply `音频转录结果：`, `转录内容`, `识别语言`, and the fixed
   local-processing notice. Reuse the existing redaction functions; do not
   translate, invoke another model, expose internal data, or change the
   Analyzer/Ticket contracts.
3. Treat empty, malformed, untrusted, or unrenderable audio output as an
   explicit presentation failure. Never fall back to `媒体处理已完成。` for audio.
4. Add contract tests for good, missing-language, empty, malformed, out-of-root,
   JSON-shaped, redacted, and long transcript cases; preserve image and video
   behavior.
5. Run focused tests, full Python, Schema, explicit Pester, `pip check`,
   `py_compile`, `git diff --check`, and scoped secret/large-file scans. Stop
   and reverse only 062A source/test changes if any required check fails.
6. Complete an independent read-only review before publishing the offline
   readiness report.

## 062B — Deferred real R4

Only after 062A passes and Jovi gives the exact start phrase: inspect the
existing Core route and Project Gateway=0, then have Jovi upload the single
fixture `tests/fixtures/feishu_delivery/p0-audio-test.wav` in the original
group without caption. Jovi sends the fresh exact `/vf audio <new-ticket>` in
that group only. The Ticket must not be copied to Codex.

Accept only one quarantined receipt, one Ticket consumption, one request, one
local faster-whisper CUDA transcript under the GPU lock, no model download or
cloud fallback, a semantic fixture match, and one visible `音频转录结果：` reply.
Any Ticket exposure is cancelled without analysis. No R5 or P0 Gate follows.
