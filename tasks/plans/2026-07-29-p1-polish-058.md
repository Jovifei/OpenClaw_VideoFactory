# P1-POLISH-058 — Offline Candidate Improvement Execution Plan

Authorization: Jovi's `PLEASE IMPLEMENT THIS PLAN` message on 2026-07-29.

## Boundary

This plan improves only the offline P1 candidate. `PROJECT_STATUS.yaml` remains
unchanged; production `scripts/factory.py` remains fail-closed; and no OpenClaw,
Feishu, Gateway, Binding, Agent, Cron, OAuth, model, commit, push, tag, or
destructive retention action is permitted.

## Ordered work

1. **058A — Compatibility qualification.** Record the current Remotion/TypeScript
   path, test a no-alias TS7 path and a newer candidate only in isolated locations,
   and retain the compatibility alias unless every required check passes.
2. **058B — Contract v2.** Add 25–60 second duration data, an idempotent SQLite
   migration, v2 render input, variable composition metadata, and the candidate
   duration/inventory/retention CLI commands.
3. **058C — Audio and captions.** Use Edge stream boundaries when available, make
   SAPI fallback reasons safe and structured, normalize public-fixture audio, and
   enforce caption timing and protected-term rules.
4. **058D — Visual polish.** Add deterministic design tokens, scene-aware template
   layouts, controlled mascot actions, still/contact-sheet generation, and
   provisional visual regression evidence.
5. **058E — Metrics and quality.** Capture redacted render/encoder metrics, run
   the bounded concurrency benchmark, and add media/audio/caption quality gates.
6. **058F — Retention and dry-run delivery.** Produce non-destructive inventory and
   retention plans while retaining the network-free idempotent delivery adapter.
7. **058G — Candidate evidence.** Render all required long-form fixture packages,
   run regressions, emit reports, and update the Obsidian validation/improvement
   records.

## Per-increment rule

Before source changes, create `reports/change_requests/P1-POLISH-058<letter>.json`.
Run that increment's targeted validation before starting the next. On a deviation,
stop, retain evidence, and report `P1_POLISH_BLOCKED:<layer>` rather than weaken a
gate or make a speculative second change.

## Completion

Only report `P1_POLISH_CANDIDATE_READY_OFFLINE` after all 058B–058G evidence is
present and all required tests pass. This is not a P1 promotion.
